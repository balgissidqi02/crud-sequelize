// import express from "express";
// import logger from "./middlewares/logger.js";

// const app = express();
// const port = 3000;

// app.use(logger); // <- middleware global

// app.get("/", (req, res) => {
//   res.send("Halo dari middleware!");
// });

// app.listen(port, () => {
//   console.log(`Server jalan di http://localhost:${port}`);
// });
